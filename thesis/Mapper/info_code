# This Python file uses the following encoding: utf-8

# if __name__ == "__main__":
#     pass

#self.Button1.clicked.connect(lambda: self.clicky("Button clicked"))
#def clicky(self, text):
#self.label1.setText(text)
#self.label1.adjustSize()
'''
        self.Foward.clicked.connect(lambda: self.move_forward("Move Forward"))
        self.Backward.clicked.connect(lambda: self.move_backward("Move Backward"))
        self.Right.clicked.connect(lambda: self.move_right("Move Right"))
        self.Left.clicked.connect(lambda: self.move_left("Move Left"))
        self.Break.clicked.connect(lambda: self.stop("Stop"))

    def move_forward(self, text):
        self.Speed.setText(text)
        self.Speed.adjustSize()

    def move_backward(self, text):
        self.Speed.setText(text)
        self.Speed.adjustSize()
    
    def move_right(self, text):
        self.Speed.setText(text)
        self.Speed.adjustSize()
    
    def move_left(self, text):
        self.Speed.setText(text)
        self.Speed.adjustSize()
    
    def stop(self, text):
        self.Speed.setText(text)
        self.Speed.adjustSize()
'''